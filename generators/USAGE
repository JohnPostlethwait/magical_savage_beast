This generator will place the controllers, models, helpers, views and routes 
into your rails application.

Example:
    The only argument taken is the name of the model that represents your 
    users. IE:

    ./script/generate magical_savage_beast user

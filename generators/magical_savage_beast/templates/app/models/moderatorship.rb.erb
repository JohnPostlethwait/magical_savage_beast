class Moderatorship < ActiveRecord::Base
  belongs_to :forum
  belongs_to :<%= singular_name %>
  before_create { |r| count(:id, :conditions => ['forum_id = ? and <%= singular_name %>_id = ?', r.forum_id, r.<%= singular_name %>_id]).zero? }
end
